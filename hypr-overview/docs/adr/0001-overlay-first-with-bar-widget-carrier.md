# Overlay-first Overview with bar widget carrier

Overview is hosted as an Omarchy Shell overlay, with a small bar widget kept only as a launcher and settings carrier. An overlay-only plugin would match the runtime surface more directly, but Omarchy Shell currently exposes third-party settings schemas through bar-widget entries; pairing the overlay with a minimal widget preserves a proper settings path without making the widget own the Overview experience.

# Use `habibe.overview` as the Omarchy plugin id

The Omarchy port uses `habibe.overview` as its stable plugin id even though the source plugin was named `hypr-overview`. The user-facing concept is simply Overview, and the namespaced id keeps the shell configuration aligned with that language while still making ownership explicit through the `habibe.` prefix.

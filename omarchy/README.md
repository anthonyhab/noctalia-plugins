This is a development copy of an Omarchy integration plugin for Noctalia.

**Install**

- Copy this folder to `~/.config/noctalia/plugins/omarchy/`
- Ensure `~/.config/noctalia/plugins.json` enables the plugin (or enable it in Settings → Plugins)

This plugin uses the core `ColorSchemeService` external scheme hook to apply colors.
